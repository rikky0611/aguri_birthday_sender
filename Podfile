# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Aguri_Birthday_Sender' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Aguri_Birthday_Sende
  pod 'Firebase'
  pod 'Firebase/Database'

  target 'Aguri_Birthday_SenderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Aguri_Birthday_SenderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
